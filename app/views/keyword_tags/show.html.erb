<p id="notice"><%= notice %></p>

<p>
  <strong>Tags:</strong>
  <h2><%= link_to @tag.name, keyword_tag_path(@tag) %></h2>
</p>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Text</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @tag.articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.text.first(20) %></td>
        <td><%= link_to 'Show', article %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', :back %>